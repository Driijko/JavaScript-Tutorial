    // Analyzing Prototype Relationships /////////////////////
    // info.tag("The prototype of rec 1:", Object.getPrototypeOf(rec1));
    // info.tag(
    //     "The prototype of the prototeype of rec 1:",
    //     Object.getPrototypeOf(Object.getPrototypeOf(rec1))
    // );
    // info.tag(
    //     "The rec object is the prototype of rec 1?",
    //     rec.isPrototypeOf(rec1)
    // );